import {JSONToCSS} from './json-to-css';

describe('JSONToCSS', () => {
	describe('should handle key-value pairs', () => {
		it('at top level', () => {});
		
		it('nested', () => {});
	});
	
	describe('should handle @ declarations', () => {
		it('@media', () => {});
		
		it('@keyframes', () => {});
		
		it('@font-face', () => {});
		
		it('@charset', () => {});
		
		it('@supports', () => {});
		
		it('@import', () => {});
	});
	
	it('should handle nested declarations with &', () => {});
})
